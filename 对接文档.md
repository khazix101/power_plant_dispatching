# 充电站微网数字孪生仿真模型——对接文档

## 项目概述

本项目构建了基于 **Python + OpenDSS** 的充电站微网数字孪生仿真模型。实现"预测数据输入 → 潮流计算 → 运行状态输出 → **电池资产经济评价**"的动态孪生闭环，复刻离网型充电站微网运行特性，并以日净利润为最终评价指标。

### 核心能力

| 能力                      | 说明                                                         |
| ------------------------- | ------------------------------------------------------------ |
| OpenDSS 微网拓扑建模      | 光伏、风电、充电桩负荷、储能、简易线路的标准化配电仿真模型     |
| Python 控制时序仿真        | 每1小时为一个Snapshot步长，Python设置功率 → OpenDSS解潮流 → 提取结果 |
| 场景调度引擎              | 日级+小时级场景匹配，基策略+修正策略流水线                     |
| **电池资产经济评价（NEW）** | SOH三组管理、日历老化+循环老化成本、日净利润计算               |
| 结果表格输出              | 功率平衡表、储能状态表、经济评价表、汇总统计                   |

---

## 1. 环境配置

### 1.1 依赖安装

```bash
pip install -r requirements.txt
```

| 包            | 版本      | 用途                                                  |
| ------------ | ------- | --------------------------------------------------- |
| `dss-python` | ≥0.14.0 | OpenDSS Python 绑定（内置 OpenDSS 引擎 DLL，Windows 无需额外安装） |
| `pandas`     | ≥1.5.0  | CSV 数据读写                                            |
| `matplotlib` | ≥3.5.0  | 架构拓扑图生成                                             |

### 1.2 验证安装

```bash
python -c "from dss import DSS; print('OpenDSS OK')"
```

---

## 2. 项目结构

```
power_plant_dispatching/
├── data/                        # 上游预测数据
│   ├── A.csv                    # 风力发电预测：valid_time, power_kW（24行）
│   ├── B.csv                    # 光伏发电预测：valid_time, power_kW（24行）
│   ├── C.csv                    # 负荷预测    ：valid_time, power_kW（24行）
│   ├── D.csv                    # 天气数据    ：valid_time, temperature_c, weather_type
│   └── E.csv                    # 节假日日历  ：date (YYYY-MM-DD)
├── model/
│   └── master.dss               # OpenDSS 微网拓扑模型文件
├── src/
│   ├── main.py                  # 主仿真入口，编排完整数字孪生流程（含经济评价）
│   ├── data/
│   │   └── loader.py            # 数据加载与校验
│   ├── model/
│   │   └── opendss.py           # OpenDSS 模型接口
│   ├── strategy/
│   │   ├── storage_strategy.py  # 储能调度策略模块（接口式设计）
│   │   ├── conditions.py        # 场景条件系统
│   │   ├── scenario_defs.py     # 场景定义工厂
│   │   └── dispatcher.py        # 场景调度器
│   ├── battery/
│   │   ├── group.py             # ★ SOH三组状态管理与组迁移逻辑
│   │   ├── asset_eval.py        # ★ 日历老化 + 循环老化成本计算
│   │   └── economic_eval.py     # ★ 全系统经济评价（日净利润）
│   ├── output/
│   │   └── writer.py            # 结果写入 CSV 表格（含经济评价输出）
│   └── utils/
│       └── diagram.py           # 微网拓扑架构图生成
├── output/                      # 仿真结果输出（自动创建）
│   ├── power_balance.csv        # 逐小时功率平衡表
│   ├── storage_state.csv        # 储能 SOC 状态表
│   ├── economic.csv             # ★ 逐日经济评价表
│   ├── summary.csv              # 汇总统计表
│   └── topology.png             # 微网拓扑架构图
```

---

## 3. 输入数据格式

### 3.1 上游对接文件清单

| 文件名     | 含义     | 列定义                                            |
| ------- | ------ | ---------------------------------------------- |
| `A.csv` | 风力发电预测 | `valid_time`（ISO 8601 时间戳）, `power_kW`（浮点数，kW） |
| `B.csv` | 光伏发电预测 | 同上                                             |
| `C.csv` | 负荷预测   | 同上                                             |
| `D.csv` | 天气数据   | `valid_time`, `temperature_c` (浮点), `weather_type` (sunny/cloudy/overcast/rainy/storm) |
| `E.csv` | 节假日日历 | `date` (YYYY-MM-DD 格式)                           |

### 3.2 自动校验规则（`data/loader.py`）

- 每个功率文件必须恰好 **24 行**
- `power_kW` 必须为 **非负数**
- 负荷 `power_kW` 不得超过充电桩总配置容量 **14,400 kW**

---

## 4. 微网拓扑与参数

### 4.1 拓扑结构

```
                       ┌─────────────────┐
                       │  Vsource (Slack) │  ← 电压参考节点
                       └────────┬────────┘
                                │
                   ┌────────────┴────────────┐
                   │     MainBus (0.4 kV)     │
                   └───┬──────┬──────┬────┬──┘
               0.05km  │      │      │    │  0.05km
                    ┌──┴──┐ ┌─┴──┐ ┌─┴──┐┌┴─────┐
                    │ PV  │ │Wind│ │Load││Storage│
                    │3000 │ │2000│ │EV  ││3750kW │
                    │ kW  │ │ kW │ │    ││7500kWh│
                    └─────┘ └────┘ └────┘└───────┘
```

### 4.2 关键参数

| 参数        | 值                   | 说明                     |
| --------- | ------------------- | ---------------------- |
| 母线额定电压    | 0.4 kV              | 中国充电站标准低压交流母线          |
| 系统频率      | 50 Hz               | 中国工频                   |
| 光伏额定容量    | 3000 kW             | Generator 等效建模         |
| 风电额定容量    | 2000 kW             | Generator 等效建模         |
| 充电桩总配置容量  | 14,400 kW           | 120 台 × 120 kW/台（负荷上限） |
| 储能 PCS 功率 | 3750 kW             | 充放电最大功率                |
| 储能容量      | 7500 kWh            | 额定能量                   |
| 线路长度      | 0.05 km (50m)       | 各设备到母线的连接电缆等效长度        |
| 线路阻抗      | R=0.01, X=0.01 Ω/km | 低压母线级连接（低阻抗）           |

---

## 5. 运行方式

### 5.1 基础运行

```bash
cd power_plant_dispatching
python src/main.py
```

### 5.2 全部命令行参数

| 参数            | 默认值                | 说明                         |
| --------------- | ------------------ | ---------------------------- |
| `--data`        | `data/`            | 预测数据目录                   |
| `--model`       | `model/master.dss` | OpenDSS 模型文件路径           |
| `--output`      | `output/`          | 结果输出目录                   |
| `--strategy`    | `simple_balancing` | 储能调度策略名称（场景模式下自动忽略） |
| `--init-soc`    | `50.0`             | 储能初始 SOC（%，范围 10~90）   |
| `--init-soh`    | `100.0`            | ★ 储能初始 SOH（%，范围 70~100）|
| `--date`        | （从数据推导）       | 仿真日期 YYYY-MM-DD            |
| `--diagram`     | `False`            | 是否生成架构拓扑图              |
| `--ndays`       | `1`                | ★ 多日仿真天数，使用同日数据重复  |
| `--no-economic` | `False`            | ★ 禁用电池资产经济评价          |

### 5.3 使用示例

```bash
# 基本运行
python src/main.py

# 自定义初始 SOC + SOH
python src/main.py --init-soc 70 --init-soh 95

# 30日仿真（观察 SOH 衰减趋势）
python src/main.py --ndays 30 --init-soh 98

# MPC 经济最优调度
python src/main.py --strategy mpc_grid13_sb

# MPC 多日仿真（观察策效衰减）
python src/main.py --strategy mpc_grid13_sb --ndays 7

# 生成拓扑图
python src/main.py --diagram

# 仅功率仿真，跳过经济评价
python src/main.py --no-economic
```

---

## 6. 电池资产经济评价体系 ★

### 6.1 设计思路（对齐 soc_health_v3.pdf）

将储能电池视为**可折旧资产**，通过以下模型将技术指标统一折算为经济成本：

```
日净利润 = EV充电收入 + 余电上网收入 − 外网购电成本 − 电池日使用成本 − 弃电惩罚
```

其中**电池日使用成本**由两部分构成：

| 成本类型 | 物理机制 | 影响因素 |
| -------- | -------- | -------- |
| 日历老化 | 高 SOC 存储加速 SEI 膜增长 | 日平均 SOC 水平 |
| 循环老化 | 充放电吞吐量引起电极材料损耗 | 日吞吐量 × DOD 非线性加权 |

### 6.2 SOH三组划分

| 组 | SOH 范围 | 角色 | 当前单电池场景 |
| -- | -------- | ---- | ----------- |
| G1（高） | [93%, 100%] | 优质资产 | 新电池处于此组 |
| G2（中） | [82%, 93%) | 常规主力 | SOH 降至此范围自动迁入 |
| G3（低） | [70%, 82%) | 劣质资产 | 接近退役 |

SOH < 70% 触发退役置换：旧电池回收 5% 残值，新电池（SOH=100%）归入 G1。

### 6.3 日历老化模型

年老化率 $k_{cal}$ 基于日均 SOC 查表：

| SOC 区间 | 年老化率 |
| -------- | -------- |
| [10%, 50%) | 1.0%/年 |
| [50%, 80%) | 1.5%/年 |
| [80%, 90%) | 2.5%/年 |
| [90%, 100%] | 4.0%/年 |

日 SOH 衰减 = $k_{cal} / 365$

### 6.4 循环老化模型

$$\Delta \text{SOH}_{cyc} \approx \gamma \cdot E_{throughput} \cdot \text{DOD}_{eff}^{\alpha-1}$$

- $\gamma$：退化系数（默认 $1.5\times10^{-8}$ /kWh，需标定）
- $\alpha$：DOD 非线性指数（默认 1.5）
- $E_{throughput}$：日能量吞吐量 (kWh)
- $\text{DOD}_{eff}$：等效放电深度 = (max_SOC − min_SOC)/100

### 6.5 经济参数表

> **注意**：Vsource 在孤岛模式中为纯电压参考节点，非实际电网连接。下表"购电/售电"实为**不平衡惩罚系数**——Vsource 功率越偏离零，惩罚越大，本质是激励调度策略维持功率自平衡。

| 参数 | 符号 | 默认值 | 语义 |
| ---- | ---- | ------ | ---- |
| EV 充电单价 | $\lambda_{EV}$ | 1.5 元/kWh | — |
| 缺额惩罚系数 | $\lambda_{deficit}$ | 0.8 元/kWh | Vsource>0 时等价惩罚 |
| 盈余抵免系数 | $\lambda_{surplus}$ | 0.4 元/kWh | Vsource<0 时等价抵免 |
| 弃电惩罚系数 | $w_{curt}$ | 0.2 元/kWh | — |
| 新电池成本 | $C_{new}$ | 750 万元 | — |
| 退役残值回收率 | $r_{salvage}$ | 5% | — |
| SOH 退役阈值 | $SOH_{retire}$ | 70% | — |

### 6.6 SOH 单日恒定简化

在单日仿真中，SOH 日衰减量约为 0.01%，对候选决策的对比排序影响可忽略。
因此 **MPC 策略内部将 SOH 视为单日恒定**，仅在经济评价阶段（事后）更新 SOH。
多日仿真中每日仿真结束后更新 SOH，次日 MPC 使用最新 SOH 进行优化。

### 6.7 MPC 经济最优调度（`mpc_grid13_sb`）★

采用基于网格搜索的滚动优化策略，在每个决策时步用经济成本驱动储能调度：

**算法原理：**

```
compute(hour=h, soc_pct=s):
  for cand in [-3750, -3125, ..., 3750] (13 档均匀分布):
    ├── 本步: 施加候选功率 cand → 得到 new_soc
    ├── 向前: 用 SimpleBalancing 模拟 hour+1 ~ 23 的 SOC 轨迹
    ├── 老化: 累加剩余小时日历老化 + 循环老化成本
    └── 经济: 累加剩余小时购电/售电收支
  return 净利润最高的候选功率
```

**目标函数（剩余小时，最大化）：**

$$\max \left( \lambda_{surplus} \cdot E_{surplus} - \lambda_{deficit} \cdot E_{deficit} - C_{cal} - C_{cyc} \right)$$

其中 $E_{deficit}$ 为 Vsource>0 的缺额电量，$E_{surplus}$ 为 Vsource<0 的盈余电量。
$\lambda_{deficit} > \lambda_{surplus}$ 的设计激励消纳本地新能源、减少功率不平衡。

**策略注册名：** `mpc_grid13_sb`

| 参数 | 含义 | 值 |
|------|------|-----|
| 候选档数 | 网格搜索精度 | 13 档 |
| 候选步长 | 档间功率差 | 625 kW |
| 向前模拟 | 未来小时默认策略 | SimpleBalancing |
| SOH 假设 | 单日内 | 恒定 |

**与场景调度器的关系：** MPC 策略通过 `--strategy mpc_grid13_sb` 走直连路径（路径 B），跳过场景条件评估，不依赖任何规则策略。

---

## 7. 输出文件格式

### 7.1 power_balance.csv（功率平衡表）

| 列名               | 单位   | 说明                          |
| ---------------- | ---- | --------------------------- |
| `hour`           | -    | 小时 (0~23)                   |
| `pv_kW`          | kW   | 光伏出力                        |
| `wind_kW`        | kW   | 风电出力                        |
| `load_kW`        | kW   | 负荷功率                        |
| `storage_kW`     | kW   | 储能功率（>0 放电，<0 充电）           |
| `source_kW`      | kW   | Vsource 功率（>0 购电，<0 售电） |
| `losses_kW`      | kW   | 线路总损耗                       |
| `net_balance_kW` | kW   | 功率平衡残差（接近 0 表示收敛良好）         |
| `bus_voltage_pu` | p.u. | 主母线电压标幺值                    |
| `soc_pct`        | %    | 每小时储能 SOC                    |

### 7.2 economic.csv ★（逐日经济评价）

| 字段 | 含义 |
| ---- | ---- |
| `date` | 仿真日期 |
| `profit_daily_yuan` | 日净利润（元/日） |
| `revenue_ev_yuan` | EV 充电收入（元） |
| `revenue_sell_yuan` | 余电上网收入（元） |
| `cost_buy_yuan` | 外网购电成本（元） |
| `cost_battery_yuan` | 三组电池日使用成本合计（元） |
| `cost_curt_yuan` | 弃电惩罚成本（元） |
| `total_load_kwh` | 日总负荷（kWh） |
| `total_generation_kwh` | 日总发电量（kWh） |
| `energy_throughput_kwh` | 日储能吞吐量（kWh） |
| `soh_sys_pct` | 全系统加权 SOH（%） |
| `usable_capacity_kwh` | 可用储能容量（kWh） |
| `is_retired` | 是否触发退役置换（0/1） |
| `cumulative_profit_yuan` | 累计净利润（元） |
| `cost_cal_g1/g2/g3_yuan` | 各组日历老化成本（元） |
| `cost_cyc_g1/g2/g3_yuan` | 各组循环老化成本（元） |
| `n_g1/g2/g3` | 各组电池数量 |
| `soh_g1/g2/g3_pct` | 各组 SOH（%） |

---

## 8. 数字孪生闭环流程（更新版）

```
┌──────────────┐    ┌─────────────────┐    ┌──────────────┐
│  data/*.csv  │───▶│  Python 数据加载  │───▶│  OpenDSS     │
│              │    │  (data_loader)   │    │  模型编译     │
└──────────────┘    └────────┬────────┘    └──────┬───────┘
                             │                    │
                             ▼                    ▼
                    ┌─────────────────┐  ┌─────────────────┐
                    │  场景调度 + 策略  │  │  设置 PV/Wind/   │
                    │  (dispatcher)   │  │  Load/Stor 功率  │
                    └────────┬────────┘  └────────┬────────┘
                             │                    │
                             └────────┬───────────┘
                                      ▼
                             ┌─────────────────┐
                             │  OpenDSS 解潮流   │
                             └────────┬────────┘
                                      ▼
                             ┌─────────────────┐
                             │  提取结果 + 更新   │
                             │  SOC → 下一步     │
                             └────────┬────────┘
                                      │
                          ◄── 循环 24 次 ──◄
                                      │
                                      ▼
                    ┌─────────────────────────────┐
                    │ ★ 电池资产经济评价（每日一次）  │
                    │  · 日历老化 + 循环老化成本      │
                    │  · SOH 更新 + 组迁移检查        │
                    │  · 日净利润计算                │
                    └──────────────┬──────────────┘
                                   ▼
                    ┌─────────────────────────────┐
                    │  output/ 写入                 │
                    │  · power_balance.csv          │
                    │  · economic.csv（★新增）       │
                    │  · summary.csv                │
                    └─────────────────────────────┘
```

---

## 9. 与远期多微网（A.md）的衔接

当前单体微网评价体系是 A.md 远期多微网 MPC 调度目标的退化形式：

- **三组 SOH 划分** 与 A.md 完全一致
- **退化成本计算** 使用同一公式体系，参数可无缝迁移
- 当前单体系统的 **退化系数 $\gamma$ 标定结果** 直接作为 A.md 的参数输入
- 日净利润 $\Pi_{day}$ 与 A.md 目标函数 $\min J(t)$ 数学等价（取负号后）

---

## 10. 模型假设与简化说明

| 项目        | 处理方式                                 |
| --------- | ------------------------------------ |
| 交直流转换     | **不区分**。所有电源、负荷统一等效为交流接口             |
| 换流器损耗     | **等效折算**进设备整体效率/出力系数                 |
| 线路模型      | 简化 π 型线路，阻抗参数取典型值                    |
| 离网模式      | Vsource 作为电压参考，其功率越接近 0 说明储能策略越完美平衡  |
| 时间步长      | 1 小时，Snapshot 模式（非连续时间推进）            |
| 储能 SOC 跟踪 | Python 侧独立计算，OpenDSS Storage 仅用于潮流求解 |
| **SOH 衰减（NEW）** | 日历老化基于日均 SOC 查表；循环老化基于吞吐量+DOD 非线性加权 |
| **组迁移（NEW）** | 单电池场景仅有一组非空；SOH 跨阈值时自动迁移至下一组 |
| **多日仿真（NEW）** | 使用同日数据重复仿真，逐日累积 SOH 衰减和净利润 |

---

## 11. 常见问题

**Q: 运行报错 "dss-python not installed"**  
A: 执行 `pip install dss-python`，该包内置 OpenDSS DLL（Windows），无需额外安装 OpenDSS。

**Q: 如何理解日净利润为负？**  
A: 负极可能表示当日 EV 充电收入不足以覆盖购电和电池折旧。可通过调整调度策略（如减少放电深度）优化。

**Q: 退化系数 $\gamma$ 如何标定？**  
A: 运行多日仿真（`--ndays N`），记录 SOH 衰减曲线，通过吞吐量与 SOH 变化的回归拟合得到。

**Q: 如何禁用经济评价只做功率仿真？**  
A: 使用 `--no-economic` 参数，跳过电池资产评价步骤。

**Q: 负荷超过 14,400 kW 怎么办？**  
A: `data/loader.py` 会校验并报错，`main.py` 中也会 clamp 到上限并打印 WARNING。

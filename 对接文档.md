# 充电站微网数字孪生仿真模型——对接文档

## 项目概述

本项目构建了基于 **Python + OpenDSS** 的充电站微网数字孪生仿真模型。实现"预测数据输入 → 潮流计算 → 运行状态输出"的动态孪生闭环，复刻离网型充电站微网运行特性。

### 核心能力

| 能力 | 说明 |
|------|------|
| OpenDSS 微网拓扑建模 | 光伏、风电、充电桩负荷、储能、简易线路的标准化配电仿真模型 |
| Python 控制时序仿真 | 每1小时为一个Snapshot步长，Python设置功率 → OpenDSS解潮流 → 提取结果 |
| 储能调度策略接口 | 模块化策略设计，支持策略即插即用与多策略对比 |
| 结果表格输出 | 功率平衡表、储能状态表、汇总统计，输出至 `output/` 文件夹 |

---

## 1. 环境配置

### 1.1 依赖安装

```bash
pip install -r requirements.txt
```

| 包 | 版本 | 用途 |
|----|------|------|
| `dss-python` | ≥0.14.0 | OpenDSS Python 绑定（内置 OpenDSS 引擎 DLL，Windows 无需额外安装） |
| `pandas` | ≥1.5.0 | CSV 数据读写 |
| `matplotlib` | ≥3.5.0 | 架构拓扑图生成 |

### 1.2 验证安装

```bash
python -c "from dss import DSS; print('OpenDSS OK')"
```

---

## 2. 项目结构

```
power_plant_dispatching/
├── data/                        # 上游预测数据（用户替换为实际文件）
│   ├── A.csv                    # 风力发电预测：valid_time, power_kW（24行）
│   ├── B.csv                    # 光伏发电预测：valid_time, power_kW（24行）
│   └── C.csv                    # 负荷预测    ：valid_time, power_kW（24行）
├── model/
│   └── master.dss               # OpenDSS 微网拓扑模型文件（可单独在 OpenDSS 中打开）
├── src/
│   ├── main.py                  # 主仿真入口，编排完整数字孪生流程
│   ├── data_loader.py           # 数据加载与校验
│   ├── opendss_model.py         # OpenDSS 模型接口（编译、设功率、求解、取结果）
│   ├── storage_strategy.py      # ★ 储能调度策略模块（接口式设计）
│   ├── output_writer.py         # 结果写入 CSV 表格
│   └── diagram.py               # 微网拓扑架构图生成
├── output/                      # 仿真结果输出（自动创建）
│   ├── power_balance.csv        # 逐小时功率平衡表
│   ├── storage_state.csv        # 储能 SOC 状态表
│   ├── summary.csv              # 汇总统计表
│   └── topology.png             # 微网拓扑架构图
└── requirements.txt
```

---

## 3. 输入数据格式

### 3.1 上游对接文件清单

文件统一放在 `data/` 目录下，各包含 24 行（对应 24 小时，1h 步长）。

| 文件名 | 含义 | 列定义 |
|--------|------|--------|
| `A.csv` | 风力发电预测 | `valid_time`（ISO 8601 时间戳）, `power_kW`（浮点数，kW） |
| `B.csv` | 光伏发电预测 | 同上 |
| `C.csv` | 负荷预测 | 同上 |

### 3.2 示例

```csv
valid_time,power_kW
2024-01-01 00:00:00,800.0
2024-01-01 01:00:00,900.0
...
2024-01-01 23:00:00,1200.0
```

### 3.3 自动校验规则（`data_loader.py`）

- 每个文件必须恰好 **24 行**
- `power_kW` 必须为 **非负数**
- 负荷 `power_kW` 不得超过充电桩总配置容量 **14,400 kW**（120 台 × 120 kW/台）

---

## 4. 微网拓扑与参数

### 4.1 拓扑结构

```
                      ┌─────────────────┐
                      │  Vsource (Slack) │  ← 电压参考节点（离网模式下功率应接近0）
                      └────────┬────────┘
                               │
                  ┌────────────┴────────────┐
                  │     MainBus (0.4 kV)     │
                  └───┬──────┬──────┬────┬──┘
              0.05km  │      │      │    │  0.05km
                   ┌──┴──┐ ┌─┴──┐ ┌─┴──┐┌┴─────┐
                   │ PV  │ │Wind│ │Load││Storage│
                   │3000 │ │2000│ │EV  ││3750kW │
                   │ kW  │ │ kW │ │    ││7500kWh│
                   └─────┘ └────┘ └────┘└───────┘
```

运行 `python src/main.py --diagram` 可在 `output/topology.png` 中生成可视化架构图。

### 4.2 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 母线额定电压 | 0.4 kV | 中国充电站标准低压交流母线 |
| 系统频率 | 50 Hz | 中国工频 |
| 光伏额定容量 | 3000 kW | Generator 等效建模 |
| 风电额定容量 | 2000 kW | Generator 等效建模 |
| 充电桩总配置容量 | 14,400 kW | 120 台 × 120 kW/台（负荷上限） |
| 储能 PCS 功率 | 3750 kW | 充放电最大功率 |
| 储能容量 | 7500 kWh | 额定能量 |
| 线路长度 | 0.05 km (50m) | 各设备到母线的连接电缆等效长度 |
| 线路阻抗 | R=0.01, X=0.01 Ω/km | 低压母线级连接（低阻抗） |

---

## 5. 运行方式

### 5.1 基础运行

```bash
cd power_plant_dispatching
python src/main.py
```

### 5.2 全部命令行参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--data` | `data/`（相对于项目根） | 预测数据目录 |
| `--model` | `model/master.dss` | OpenDSS 模型文件路径 |
| `--output` | `output/` | 结果输出目录 |
| `--strategy` | `simple_balancing` | 储能调度策略名称 |
| `--init-soc` | `50.0` | 储能初始 SOC（%），范围 10~90 |
| `--diagram` | `False` | 是否生成架构拓扑图 |

### 5.3 使用示例

```bash
# 基本运行
python src/main.py

# 自定义初始 SOC 并生成拓扑图
python src/main.py --init-soc 70 --diagram

# 指定自定义数据目录
python src/main.py --data my_data/

# 使用自定义策略
python src/main.py --strategy my_custom_strategy
```

---

## 6. 储能调度策略模块（接口式设计）★

### 6.1 设计思路

策略模块独立为 `src/storage_strategy.py`，主程序在 `src/main.py` 中通过**工厂函数 + 命令行参数**调用策略。目的是：

- **即插即用**：增加新策略时，仅需在策略文件中新增一个类并在注册表中登记
- **零侵入主程序**：主程序循环只调用 `strategy.compute()`，不感知策略内部逻辑
- **多策略对比**：通过 `--strategy` 参数切换策略，可同一数据多次运行对比结果

### 6.2 接口定义

所有策略必须继承 **`StorageStrategy`** 抽象基类，并实现 **`compute()`** 方法：

```python
from abc import ABC, abstractmethod

class StorageStrategy(ABC):
    @abstractmethod
    def compute(self, pv_kw, wind_kw, load_kw, soc_pct,
                rated_capacity_kwh, rated_power_kw, time_hour):
        """
        计算单步储能充放电功率。

        参数（均为 float）：
            pv_kw (float)            : 当前时刻光伏出力 (kW)
            wind_kw (float)          : 当前时刻风电出力 (kW)
            load_kw (float)          : 当前时刻负荷功率 (kW)
            soc_pct (float)          : 当前储能 SOC (%)
            rated_capacity_kwh (float): 储能额定容量 (kWh)
            rated_power_kw (float)   : 储能 PCS 额定功率 (kW)
            time_hour (int)          : 当前小时 (0~23)

        返回值 (tuple)：
            (storage_kW: float, new_soc_pct: float)
                storage_kW > 0 → 放电
                storage_kW < 0 → 充电
                new_soc_pct  → 更新后的 SOC (%)
        """
        pass
```

### 6.3 主循环调用方式

在 `src/main.py` 第 105~113 行，每个时间步执行：

```python
storage_kw, storage_soc = strategy.compute(
    pv_kw=pv_kw,
    wind_kw=wind_kw,
    load_kw=load_kw,
    soc_pct=storage_soc,          # 上一时刻的 SOC
    rated_capacity_kwh=STORAGE_KWH,
    rated_power_kw=STORAGE_KW,
    time_hour=hour,
)
```

策略返回的 `storage_kW` 直接传入 OpenDSS 模型设置储能功率，`new_soc_pct` 缓存为下一时刻 SOC。

### 6.4 已实现策略：SimpleBalancing（供需平衡）

```python
class SimpleBalancing(StorageStrategy):
    """
    逻辑：
      - 光伏+风电 > 负荷 → 以多余功率充电（受 PCS 功率和 SOC 上限约束）
      - 光伏+风电 < 负荷 → 以缺额功率放电（受 PCS 功率和 SOC 下限约束）
      - 充放电效率：95%（可配置）
      - SOC 范围：10%~90%（可配置）
    """
```

### 6.5 如何添加新策略（三步走）

**Step 1**：在 `src/storage_strategy.py` 中新建类，继承 `StorageStrategy`，实现 `compute()` 方法：

```python
class PeakShavingStrategy(StorageStrategy):
    """削峰填谷策略：在电费高峰期放电，低谷期充电"""
    
    def __init__(self, peak_start=8, peak_end=20, soc_min=10, soc_max=90):
        self.peak_start = peak_start
        self.peak_end = peak_end
        self.soc_min = soc_min
        self.soc_max = soc_max
    
    def compute(self, pv_kw, wind_kw, load_kw, soc_pct,
                rated_capacity_kwh, rated_power_kw, time_hour):
        is_peak = self.peak_start <= time_hour < self.peak_end
        
        if is_peak:
            # 高峰期：尽量放电
            discharge_power = min(rated_power_kw,
                                  (soc_pct - self.soc_min) / 100 * rated_capacity_kwh)
            new_soc = soc_pct - discharge_power / rated_capacity_kwh * 100
            return discharge_power, new_soc
        else:
            # 低谷期：尽量充电
            charge_power = min(rated_power_kw,
                               (self.soc_max - soc_pct) / 100 * rated_capacity_kwh)
            new_soc = soc_pct + charge_power / rated_capacity_kwh * 100
            return -charge_power, new_soc
```

**Step 2**：在 `STRATEGY_REGISTRY` 注册表中登记：

```python
STRATEGY_REGISTRY = {
    "simple_balancing": SimpleBalancing,
    "peak_shaving": PeakShavingStrategy,    # ← 添加这一行
}
```

**Step 3**：运行时通过 `--strategy` 切换：

```bash
python src/main.py --strategy peak_shaving
```

> **注意**：若新策略需要额外的超参数（如 `peak_start`），可在策略类的 `__init__` 中设置默认值。如需通过命令行传入，在 `src/main.py` 的 `parse_args()` 中添加对应参数，并在构造策略时通过 `**kwargs` 传递（`get_strategy()` 函数已支持）。

---

## 7. 输出文件格式

### 7.1 power_balance.csv（功率平衡表）

| 列名 | 单位 | 说明 |
|------|------|------|
| `hour` | - | 小时 (0~23) |
| `pv_kW` | kW | 光伏出力 |
| `wind_kW` | kW | 风电出力 |
| `load_kW` | kW | 负荷功率 |
| `storage_kW` | kW | 储能功率（>0 放电，<0 充电） |
| `source_kW` | kW | Vsource 功率（>0 消纳盈余，<0 弥补缺额） |
| `losses_kW` | kW | 线路总损耗 |
| `net_balance_kW` | kW | 功率平衡残差（接近 0 表示收敛良好） |
| `bus_voltage_pu` | p.u. | 主母线电压标幺值 |

### 7.2 storage_state.csv（储能状态表）

| 列名 | 单位 | 说明 |
|------|------|------|
| `hour` | - | 小时 |
| `soc_pct` | % | 当前 SOC |
| `storage_kW` | kW | 当前储能功率 |

### 7.3 summary.csv（汇总统计）

包含：总光伏发电量、总风力发电量、总负荷消纳量、总线损、初/末 SOC、负荷越限次数。

---

## 8. 数字孪生闭环流程

```
┌──────────────┐    ┌─────────────────┐    ┌──────────────┐
│  data/A.csv  │───▶│  Python 数据加载  │───▶│  OpenDSS     │
│  data/B.csv  │    │  (data_loader)   │    │  模型编译     │
│  data/C.csv  │    └────────┬────────┘    └──────┬───────┘
└──────────────┘             │                    │
                             ▼                    ▼
                    ┌─────────────────┐  ┌─────────────────┐
                    │  储能策略计算     │  │  设置 PV/Wind/   │
                    │  (strategy)     │  │  Load/Stor 功率  │
                    └────────┬────────┘  └────────┬────────┘
                             │                    │
                             └────────┬───────────┘
                                      ▼
                             ┌─────────────────┐
                             │  OpenDSS 解潮流   │
                             │  (Snapshot Mode) │
                             └────────┬────────┘
                                      ▼
                             ┌─────────────────┐
                             │  提取结果：       │
                             │  · 母线电压       │
                             │  · Vsource 功率   │
                             │  · 线路损耗       │
                             └────────┬────────┘
                                      ▼
                             ┌─────────────────┐
                             │  记录结果 + 更新   │
                             │  SOC → 下一步     │
                             └────────┬────────┘
                                      │
                          ◄── 循环 24 次 ──◄
                                      │
                                      ▼
                             ┌─────────────────┐
                             │  output/ 写入     │
                             │  CSV + 拓扑图     │
                             └─────────────────┘
```

---

## 9. 模型假设与简化说明

| 项目 | 处理方式 |
|------|----------|
| 交直流转换 | **不区分**。所有电源、负荷统一等效为交流接口 |
| 换流器损耗 | **等效折算**进设备整体效率/出力系数 |
| 线路模型 | 简化 π 型线路，阻抗参数取典型值 |
| 离网模式 | Vsource 作为电压参考，其功率越接近 0 说明储能策略越完美平衡 |
| 时间步长 | 1 小时，Snapshot 模式（非连续时间推进） |
| 储能 SOC 跟踪 | Python 侧独立计算，OpenDSS Storage 仅用于潮流求解 |

---

## 10. 常见问题

**Q: 运行报错 "dss-python not installed"**  
A: 执行 `pip install dss-python`，该包内置 OpenDSS DLL（Windows），无需额外安装 OpenDSS。

**Q: 如何修改微网拓扑（如增删设备）？**  
A: 编辑 `model/master.dss` 和 `src/opendss_model.py` 中的 `set_power()`、`get_line_losses_kw()` 等方法。

**Q: 如何改为与主电网并网运行？**  
A: 将 Vsource 理解为并网点（PCC），其功率即代表与主电网的交换功率；不需要修改模型。

**Q: 负荷超过 14,400 kW 怎么办？**  
A: `data_loader.py` 会校验并报错，`main.py` 中也会 clamp 到上限并打印 WARNING。
